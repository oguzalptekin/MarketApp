<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.oguz.marketapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MarketApp">
        <activity android:name=".LoadingScreen"></activity>
        <activity android:name=".CosmeticsActivity" />
        <activity android:name=".PetFoodActivity" />
        <activity android:name=".StapleActivity" />
        <activity android:name=".BakerActivity" />
        <activity android:name=".TechnologyActivity" />
        <activity android:name=".FoodActivity" />
        <activity android:name=".OthersActivity" />
        <activity android:name=".GreenGrocerActivity" />
        <activity android:name=".DrinkActivity" />
        <activity android:name=".SnackActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".SignUpActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>